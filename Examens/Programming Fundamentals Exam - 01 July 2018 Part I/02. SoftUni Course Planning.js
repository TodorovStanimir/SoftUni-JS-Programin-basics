function SoftUniCoursePlanning(input) {
let plan=input.shift();
let first=plan[0];
let second=plan[1];
let third=plan[2];
console.log(plan);
}
SoftUniCoursePlanning(['Data Types, Objects, Lists'], 'Add: Databases', 'Insert: Arrays: 0',
    'Remove: Lists', 'course start')